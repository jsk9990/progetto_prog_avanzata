--------TO DO LIST -------------

1. CREARE MODELLO RICHIESTE ------> FATTO
2. CREARE MODELLO SIMULAZIONE ------> FATTO
3. CREAZIONE CONNESSIONE E TEST MYSQL SERVER 
4. CREAZIONE FUNZIONE DI LOGIN 
5. CREARE MIDDLEWARE CHECKLOGIN 
6. CREARE FUNZIONE DI SIGN IN 
7. CREARE MIDDLEWARE CHECKADMIN
8. VEDERE DI IMPLEMENTARE JWT PER IL TOKEN






------------MODIFICHE EFFETTUATE 

Cambiato lo script sql per la creazione della tabella Archi, perchè 
gli dava fastidio che la chiave esterna per nodo partenza e nodo arrivo 
era lo stesso nome 

    CODICE VECCHIO : 

    CREATE TABLE IF NOT EXISTS `mydb`.`Archi` (
  `id_archi` INT NOT NULL AUTO_INCREMENT,
  `id_grafo` INT NULL,
  `id_nodo_partenza` INT NOT NULL,
  `id_nodo_arrivo` INT NOT NULL,
  `peso` FLOAT NOT NULL,
  PRIMARY KEY (`id_archi`),
  INDEX `id_grafo_idx` (`id_grafo` ASC) VISIBLE,
  INDEX `id_nodo_partenza_idx` (`id_nodo_partenza` ASC) VISIBLE,
  INDEX `id_nodo_arrivo_idx` (`id_nodo_arrivo` ASC) VISIBLE,
  CONSTRAINT `id_grafo`
    FOREIGN KEY (`id_grafo`)
    REFERENCES `mydb`.`Grafo` (`id_grafo`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `id_nodo_partenza`
    FOREIGN KEY (`id_nodo_partenza`)
    REFERENCES `mydb`.`Nodi` (`id_nodi`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `id_nodo_arrivo`
    FOREIGN KEY (`id_nodo_arrivo`)
    REFERENCES `mydb`.`Nodi` (`id_nodi`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
COMMENT = 'Tabella che memorizza informazioni sugli archi ';


    CODICE NUOVO: 

    CREATE TABLE IF NOT EXISTS `mydb`.`Archi` (
  `id_archi` INT NOT NULL AUTO_INCREMENT,
  `id_grafo` INT NULL,
  `id_nodo_partenza` INT NOT NULL,
  `id_nodo_arrivo` INT NOT NULL,
  `peso` FLOAT NOT NULL,
  PRIMARY KEY (`id_archi`),
  INDEX `id_grafo_idx` (`id_grafo` ASC) VISIBLE,
  INDEX `id_nodo_partenza_idx` (`id_nodo_partenza` ASC) VISIBLE,
  INDEX `id_nodo_arrivo_idx` (`id_nodo_arrivo` ASC) VISIBLE,
  CONSTRAINT `id_grafo`
    FOREIGN KEY (`id_grafo`)
    REFERENCES `mydb`.`Grafo` (`id_grafo`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `id_nodo_partenza_fk`
    FOREIGN KEY (`id_nodo_partenza`)
    REFERENCES `mydb`.`Nodi` (`id_nodi`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `id_nodo_arrivo_fk`
    FOREIGN KEY (`id_nodo_arrivo`)
    REFERENCES `mydb`.`Nodi` (`id_nodi`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
COMMENT = 'Tabella che memorizza informazioni sugli archi ';